{
  "name": "copyguard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Polymarket wallet risk agent for copy-trading allocation decisions.",
  "scripts": {
    "dev": "node src/local-server.js",
    "test": "node --test",
    "verify": "node --check src/local-server.js && node --check public/assets/app.js && node --check api/config.js && node --check api/analyze.js && node --check api/arc/verify-payment.js && node --test"
  },
  "engines": {
    "node": ">=18.17"
  }
}
